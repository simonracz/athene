#ifndef big_int_hpp
#define big_int_hpp

/*
 
#include <vector>

namespace athene {

class BigInt
{
public:
	explicit BigInt(long long value = 0);
	BigInt(char* digits, size_t size);
	
	void clear();
	size_t num_of_digits();
	
	
	
	BigInt operator+(const BigInt& theOther);
	
	
	
	
	
	
};

std::ostream& operator<< (std::ostream& os, const BigInt& bigInt);

}

*/
 
#endif